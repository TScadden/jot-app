package com.notel.notel.ui.theme

import androidx.compose.ui.graphics.Color

// ── Tile Theme — Dark Navy + Cyan (matches reference design) ──────────────────
val NotelBackground    = Color(0xFF080E1A)   // Very deep navy — page background
val NotelSurface       = Color(0xFF0D1428)   // Deep navy — tile card background
val NotelSurfaceHigh   = Color(0xFF152040)   // Slightly lighter navy — elevated surfaces
val NotelPrimary       = Color(0xFF7C6EFF)   // Purple accent — primary actions, headers
val NotelAccent        = Color(0xFF7C6EFF)   // Purple — secondary accent
val NotelTextPrimary   = Color(0xFFF0EEFF)   // Near-white — primary text
val NotelTextSecondary = Color(0xFF7A8FAF)   // Muted blue-grey — secondary text

// ── Legacy glass colors (kept for GlassySpinner / other minor uses) ───────────
val GlassWhite         = Color(0x33FFFFFF)
val GlassWhiteHigh     = Color(0x66FFFFFF)
val GlassPrimary       = Color(0x4D7C6EFF)
val GlassBorder        = Color(0x26FFFFFF)
val GlassHighlight     = Color(0x1AFFFFFF)